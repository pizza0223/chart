kind: pipeline
type: docker
name: clone

steps:
  - name: host         
    image: alpine         
    commands:                 
      - cat /etc/hosts
    when:                          
      status: [ success, failure ]

  - name: echo                    
    image: plugins/git            
    commands:               
    - echo "78523"
    when:                      
      branch:
      - master
  # 事件三
  - name: dev_action         
    image: plugins/git        
    commands:                  
    - echo "111111"
    when:                  
      branch:
      - develop

trigger:
  branch: 
  - main
  event:
  - push
